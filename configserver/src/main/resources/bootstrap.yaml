spring:
  application:
    name: config-server
# 파일 시스템 사용
  profiles:
    active: native
  cloud:
    config:
      server:
        # 로컬 구성 정보: classpath 위치나 파일 시스템 위치가 될 수 있다.
        native:
          search-locations: classpath:/config

server:
  port: 8071